# Rubydraw

Planned stack:
- Frontend: React Router (framework mode) + TypeScript + Vite, tldraw for the board, Three.js for 3D preview.
- Backend: Rails with GraphQL API (planned), persisting board snapshots and assets.
